{
  "name": "move-gaussian",
  "private": true,
  "devDependencies": {
    "@mysten/prettier-plugin-move": "^0.3.0",
    "prettier": "^3.4.2"
  },
  "scripts": {
    "fmt": "prettier --write \"sources/**/*.move\" \"tests/**/*.move\"",
    "fmt:check": "prettier --check \"sources/**/*.move\" \"tests/**/*.move\""
  }
}
